# Quarantined MySQL leftovers

These migrations target MySQL (`ENUM`, charset). Beacon OMS runs on SQL Server via `prisma db push` from `schema.prisma`.

Do not apply these files. See `prisma/README.md`.
